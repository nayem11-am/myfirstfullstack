{
  "name": "sui-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/web\" \"npm run dev -w apps/api\"",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspaces --if-present",
    "build:api": "npm install && npm run build -w apps/api && cd apps/api && npx prisma generate && npx prisma db push",
    "start:api": "npm run start -w apps/api",
    "build:web": "npm install && npm run build -w apps/web",
    "start:web": "npm run start -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@next/swc-win32-x64-msvc": "^16.2.4"
  }
}
